# defaults/engine-templates/sycl.yaml
# TEMPLATE – Copy to instances/<name>/engines/sycl.yaml.
#
# SYCL / Intel oneAPI – llama.cpp SYCL build
# Typical use case: Intel Arc GPU, Intel integrated GPU
#
# Machine-local binary paths should normally be supplied by the launcher with
# Dispatcher --bin-dir. For standalone Dispatcher use, an optional fallback can
# be declared here:
# bin_dir: "c:\\llama.cpp\\server\\server_07_SYCL"
# server_bin: "llama-server.exe"
