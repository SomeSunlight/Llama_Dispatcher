# defaults/engine-templates/vulkan.yaml
# TEMPLATE – Copy to instances/<name>/engines/vulkan.yaml.
#
# Vulkan – llama.cpp Vulkan build
# Typical use case: AMD/Intel GPU, laptop GPUs without CUDA support
#
# Machine-local binary paths should normally be supplied by the launcher with
# Dispatcher --bin-dir. For standalone Dispatcher use, an optional fallback can
# be declared here:
# bin_dir: "c:\\llama-cpp\\server\\server_vulkan"
# server_bin: "llama-server.exe"
