# defaults/engine-templates/vulkan.yaml
# TEMPLATE – Copy to instances/<name>/engines/vulkan.yaml and adjust bin_dir.
#
# Vulkan – llama.cpp Vulkan build
# Typical use case: AMD/Intel GPU, laptop GPUs without CUDA support

bin_dir: "c:\\llama-cpp\\server\\server_vulkan"
# server_bin: "llama-server.exe"   # Optional – Default is llama-server.exe
